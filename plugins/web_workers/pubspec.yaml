name: web_workers
version: 0.0.1

flutter:
  plugin:
    platforms:
      web:
        pluginClass: WebWorkersPlugin
        fileName: web_workers.dart

dependencies:
  flutter:
    sdk: flutter
  
  flutter_web_plugins:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

environment: 
   sdk: '>=2.19.5 <3.0.0'
   flutter: '>=3.0.0 <4.0.0'
